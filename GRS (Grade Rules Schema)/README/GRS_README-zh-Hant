# GRS（Grade Rules Schema）

** GRS（Grade Rules Schema，成績規則描述規範）** 是一套開放的 JSON 規範，用於統一描述各大專院校的 GPA（Grade Point Average）計算規則。

GRS 最初作為 **GPA Manager** 專案的一部分而設計，目的是讓不同學校的 GPA 制度能夠透過標準化的 JSON 文件描述，而不需要修改程式碼即可支援不同的計算方式。

每一份 GRS 文件僅描述 **一所學校** 的成績規則，任何支援 GRS 的軟體都可以直接讀取並套用。

---

# 設計理念

GRS 的設計遵循以下原則：

* **一份 JSON 對應一所學校。**
* **採用人類可讀的 JSON 格式。**
* **容易閱讀與維護。**
* **具備版本管理機制。**
* **在可行情況下保持向後相容。**
* **不允許腳本或可執行程式碼。**
* **不支援使用者自訂公式。**

---

# 功能特色

目前 GRS 支援以下成績輸入方式：

* 數值成績（Numeric）
* 文字成績（Text）
* 字母成績（Letter）
* 特殊成績（Special）

其中，數值成績目前支援三種計算模式：

* **Range**（區間對應）
* **Linear**（線性插值）
* **Lookup**（精確查表）

---

# 版本管理

GRS 採用版本化設計。

目前最新版本為：

**GRS v1**

未來若新增功能或擴充規格，將以新的版本（例如 GRS v2、GRS v3）發布，並盡可能維持向後相容。

---

# 文件

GRS 的各版本規範將以獨立文件提供。

目前包含：

* **GRS v1 規範**

未來版本將以獨立文件發布。

---

# 參考實作

GRS 的官方參考實作包含於 **GPA Manager** 專案中。

GitHub Repository：

https://github.com/tdowningsun/GPAManager

---

# 授權

GRS 作為 GPA Manager 專案的一部分，採用與 GPA Manager 相同的授權條款發佈。
